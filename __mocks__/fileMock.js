/**
 * Asset stub used by jest.config.js moduleNameMapper for binary/font
 * files (png, jpg, ttf, woff, etc.). Returning a plain string keeps
 * `require('./logo.png')` from crashing while making it obvious in
 * snapshots that a stubbed asset was used.
 */
module.exports = 'test-file-stub';